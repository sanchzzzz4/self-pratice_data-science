Program to take  area of  square from user and print the length of its side.

import math

# Take input for the area of the square
area = float(input("Enter the area of the square: "))

# Calculate the side length
side_length = math.sqrt(area)

# Print the side length
print("The length of the side is:", side_length)
