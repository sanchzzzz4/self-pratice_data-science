Program to check if a person is eligible to vote:

# Take the age of the person as input
age = int(input("Enter your age: "))

# Check if the age is 18 or above
is_eligible = age >= 18

# Print the result
print("Eligible to vote:", is_eligible)
